{
  "name": "careos-frontend",
  "version": "0.0.0",
  "scripts": {
    "start": "vite ",
    "build": "run(){ rm -rf dist; cd $(pwd)/apps/$1; vite build --base=./ --outDir ../../dist/$1 -d; }; run ",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.23"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.10.2",
    "typescript": "^4.4.4",
    "vite": "^2.7.0",
    "vue-tsc": "^0.28.10"
  }
}
